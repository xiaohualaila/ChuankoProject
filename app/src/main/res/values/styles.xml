<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- 全屏幕拉伸-->
    <style name="biovein_layout_full">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">fill_parent</item>
    </style>

    <!-- 固定自身大小-->
    <style name="biovein_layout_wrap">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <!-- 横向分布-->
    <style name="biovein_layout_horizontal" parent="biovein_layout_full">
        <item name="android:layout_width">0px</item>
        <item name="android:layout_height">@dimen/appbtn_height</item>
        <item name="android:textSize">@dimen/appbtn_text_size</item>
        <item name="android:layout_margin">0px</item>
        <item name="android:padding">0px</item>
    </style>

    <!-- 纵向分布-->
    <style name="biovein_layout_vertical" parent="biovein_layout_full">
        <item name="android:layout_height">0px</item>
    </style>

</resources>
